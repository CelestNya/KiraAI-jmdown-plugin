{
    "max_cache": {
        "type": "integer",
        "default": 10,
        "hint": "最多缓存几本 PDF，超限自动删最旧"
    },
    "desc_max_length": {
        "type": "integer",
        "default": 80,
        "hint": "描述截取字符数"
    },
    "download_threads": {
        "type": "integer",
        "default": 45,
        "hint": "下载图片的并行线程数"
    },
    "chunk_size": {
        "type": "integer",
        "default": 524288,
        "hint": "Stream 上传分片字节数，默认 512KB。调整需重启，建议 512KB-4MB"
    },
    "upload_timeout": {
        "type": "integer",
        "default": 300,
        "hint": "上传超时秒数（大文件建议 300+）"
    },
    "notify_llm": {
        "type": "switch",
        "default": true,
        "hint": "任务完成后是否在目标会话触发 LLM 自动回复"
    },
    "content_query": {
        "type": "switch",
        "default": false,
        "hint": "允许搜索和查看本子元信息。关闭后 search_jm_album 和 query_jm_album 将不可用，避免 LLM 内容审查"
    }
}
